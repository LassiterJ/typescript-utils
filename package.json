{
  "name": "typescript-utils",
  "version": "1.0.0",
  "description": "a library that contains some useful typescript utils and extendable design pattern implementations. Use this library to add control and consistency to your application keeping your code clean and readable and easier to maintain ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gts": "^3.1.1",
    "typescript": "^4.9.5",
    "@types/node": "^14.11.2"
  }
}
